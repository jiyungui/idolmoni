/* ========================================
   dock.js — Dock 栏逻辑
   ======================================== */
const Dock = (() => {

    function init() {
        // Dock 当前无需额外初始化逻辑
        // 后续可在此添加：激活状态、未读角标等
    }

    document.addEventListener('DOMContentLoaded', init);

    return {};
})();
